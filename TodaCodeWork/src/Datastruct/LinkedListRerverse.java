package Datastruct;

import javax.xml.soap.Node;

public class LinkedListRerverse {
    /*
    public Node linkedListReverse(Node head){
        if( null == head || null = head.next){
            return head;
        }
        Node temp = head.next;
        Node newHead = linkedListReverse(head.next);
        temp.next = head;
        head.next = null;
        return newHead;
    }
    * */
/*    public Node reverseLinkesList(Node head){
        if(null == head || null == head.next){
            return head;
        }
        Node temp = head.next;
        Node newHead =reverseLinkesList(temp);
        temp.next = head;

        head.next = null;
        return newHead;
    }*/
}
